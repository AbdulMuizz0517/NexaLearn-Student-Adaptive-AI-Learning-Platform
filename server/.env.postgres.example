# PostgreSQL (docker-compose default)
# Start the DB from repo root:
#   docker compose up -d
# Then set this:
DATABASE_URL=postgresql+psycopg2://nexalearn:nexalearn@localhost:5432/nexalearn

# Security
SECRET_KEY=replace-with-a-long-random-string
ALGORITHM=HS256

# AI Configuration
OLLAMA_MODEL=qwen3:0.6b
OLLAMA_API_URL=http://127.0.0.1:11434/api/generate
